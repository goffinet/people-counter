[property]
gpu-id=0

# Modèle PeopleNet v2.6 livré avec DeepStream samples
# Chemin dans l'image nvcr.io/nvidia/deepstream:9.0-samples-multiarch
tlt-encoded-model=/opt/nvidia/deepstream/deepstream/samples/models/tao_pretrained_models/peoplenet/resnet34_peoplenet_int8.etlt
tlt-model-key=tlt_encode

# Moteur TensorRT compilé au premier démarrage (~5 min) et mis en cache dans /data
model-engine-file=/data/models/peoplenet_b1_int8.engine

labelfile-path=/app/config/peoplenet_labels.txt

# Pré-traitement : normalisation [0,1]
net-scale-factor=0.0039215697906911373
model-color-format=0

# Résolution d'entrée du modèle (W=960, H=544)
infer-dims=3;544;960
maintain-aspect-ratio=1
symmetric-padding=1

batch-size=1
network-mode=1          ; 0=FP32 | 1=INT8 | 2=FP16
num-detected-classes=3  ; 0=person  1=bag  2=face
interval=0
gie-unique-id=1
process-mode=1
network-type=0          ; 0=DETECTOR
cluster-mode=2          ; NMS

[class-attrs-all]
pre-cluster-threshold=0.2
nms-iou-threshold=0.5

[class-attrs-0]
; Seuil plus élevé pour la classe "personne" → moins de faux positifs
pre-cluster-threshold=0.4
nms-iou-threshold=0.5
